<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'tweeets/profile', user: @user %>
      <%= render 'tweeets/feed', tweeets: @tweeets, user: @user %>
      <%= render 'tweeets/who_to_follow' %>
    </div>
  </div>
</section>
