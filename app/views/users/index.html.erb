<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'profile' %>
      <%= render 'feed' %>
      <%= render 'who_to_follow' %>
    </div>
  </div>
</section>
